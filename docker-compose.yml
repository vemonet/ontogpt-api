version: "3.3"
services:

  opengpt-api:
    build: .
    ports:
    - 8000:80
    environment:
      VIRTUAL_PORT: 80
      VIRTUAL_HOST: opengpt.collaboratory.semanticscience.org
      LETSENCRYPT_HOST: opengpt.collaboratory.semanticscience.org
    # env_file:
    #   - .env
